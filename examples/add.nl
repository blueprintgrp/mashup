// Nitro Language Example (add)

// Define function with name add and arguments "x" and "y"
fun add (x, y) {
    // return the result of x + y
    x + y
}

// Call the standard prinln function and call the add function
println(add(15, 5))